<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <!--标准语法-->
    <p>{{ name }}</p>
    <p>{{ 1 + 1 }}</p>
    <p>{{ 1 + 1 === 2 ? '相等' : '不相等' }}</p>
    <p>{{ @ content }}</p> <!--获取原文标签-->

    <!--标准语法-->
    <p><%= name %></p>
    <p><%= 1 + 1 %></p>
    <p><%= 1 + 1 === 2 ? '相等' : '不相等' %>></p>
    <p><%- content %></p>  <!--获取原文标签-->
</body>
</html>